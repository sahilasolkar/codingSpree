//Find numbers represented as the sum of two cubes for two different pairs

        // find the cube root of `n`
        // let cb = parseInt(Math.pow(n, 1.0 / 3))
 
        // // create an empty set
        // let s = []
 
        // for (let i = 1; i < cb - 1; i++)
        // {
        //     for (let j = i + 1; j < cb + 1; j++)
        //     {
        //         // (i, j) forms a pair
        //         let sum = (i*i*i) + (j*j*j);
 
        //         // sum is seen before
        //         if (s.includes(sum)) {
        //             console.log(sum);
        //         }
        //         else {
        //             // sum is not seen before
        //             s.push(sum);
        //         }
        //     }
        // }

console.log(CharCode(59));