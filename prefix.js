const strs = ["flower","flow","flight"]

// const temp = "rohan"
// console.log(temp.charAt(0))

const prefixlong = (arr) => {

    const newarr = []
    let temp = arr[0].charAt(0)
    for(let i=0;i<arr.length;i++)
    {
        for(let j=0;j<arr.length;j++)
        {
            
        }
        
    }
    console.log(temp)
}
prefixlong(strs)